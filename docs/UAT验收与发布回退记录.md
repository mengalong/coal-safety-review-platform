# UAT 验收与发布回退记录

## 1. 自动化 UAT

使用专用管理员账户，密码只从 `COAL_UAT_PASSWORD` 读取。基础模式创建带 `UAT-` 型号的独立任务，验证登录
会话、一个任务一个产品型号、初始轮次、资料上传、异步解析、证据块追溯和人工解析确认：

```bash
export COAL_UAT_PASSWORD='由密钥管理系统注入'
scripts/uat.sh --base-url https://coal-review.example.com:8443 \
  --login-name uat-admin --confirm-write --output artifacts/uat-basic.json
```

完整模式会调用已配置的外部模型，因此额外要求显式确认费用。它使用目标环境已有的活动标准版本，继续验证
标准确认、动态原子项、三层规则装配、版本快照、真实审核、证据问题人工确认、确认式局部重跑、标准覆盖检查、
报告发布和 Word/PDF 下载：

```bash
scripts/uat.sh --base-url https://coal-review.example.com:8443 \
  --login-name uat-admin --confirm-write --full-audit --confirm-model-cost \
  --output artifacts/uat-full.json
```

企业内部 CA 通过 `--ca-file` 指定。工具不提供跳过 TLS 验证的参数。目标环境至少需要一个含已发布解析修订的
活动标准版本、可装配的活动规则/执行器版本、可用的千帆模型配置及在线 Worker，否则完整 UAT 明确失败。
完整模式首先执行活动文本模型连通性请求；审核后至少需要一个动态原子项产生 `passed` 或 `failed` 的确定结果，
客户证据和标准证据引用必须有效，模型请求 ID 必须能在成功调用审计日志中匹配。模型失败后转人工、证据不足、
引用无效或仅生成 `unable_to_determine` 均不计为真实模型 UAT 通过。

## 2. 人工验收

自动化通过后，业务代表仍须在前端逐项确认：

- 审核人员只能看到本人任务，管理员可完成目录与运行管理。
- 任务、轮次、文件、标准覆盖和问题证据在桌面与移动宽度下无重叠或截断。
- 扫描 PDF 页缩略图、区域证据、Word 表格行和 Excel 单元格范围可回到原资料。
- 动态项不适用和转人工决策均要求理由，未确认项阻止报告发布。
- 模型不确定或证据不足时不输出确定合格结论。
- 局部重跑弹出确认范围，不覆盖无关规则执行和人工结论。
- 报告 Word/PDF 中文、页码、表格、长文本和打印效果符合交付样张。
- 模型密钥、指标令牌、用户令牌在页面、API、日志和错误信息中均不回显。

## 3. 缺陷收敛

缺陷按 `阻断上线 / 高 / 中 / 低` 记录，必须包含候选 Commit、复现步骤、trace ID、预期与实际结果、修复提交
和回归证据。上线门槛为：阻断上线和高等级缺陷为零；中等级缺陷均有业务与运维共同接受的处置计划；所有修复
重新通过后端、前端、Compose、安全基线、基础 UAT 和受影响专项测试。

## 4. 发布与回退演练

发布使用固定镜像摘要和 `scripts/release.sh`。脚本强制运行 UAT 并在失败时终止发布，成功或失败均记录联合备份
路径、发布耗时、镜像摘要和 UAT 结果文件。随后使用上一版本环境文件和本次发布前备份执行
`scripts/rollback.sh`，再次强制运行冒烟与 UAT，确认任务、对象、会话和报告可读取。演练结束后可重新发布候选版本。
隔离环境可以使用 `--local-drill` 切换到预构建的上一 Commit 镜像并完整恢复数据，但该模式会在记录中标记跳过
远程摘要拉取，只能证明回退机械流程，不能替代目标环境不可变镜像签署。
冒烟脚本会在限定时间内等待 Web 和 Worker 健康状态收敛，避免把正常容器冷启动误判为回退失败；真正的
`unhealthy`、退出和重启状态仍立即阻断流程。

## 5. 签署记录

| 字段 | 记录 |
|---|---|
| 候选 Commit / API 镜像摘要 / Web 镜像摘要 | 待填写 |
| 基础 UAT 结果文件 | 待填写 |
| 完整 UAT 结果文件 | 待填写 |
| 未关闭缺陷及接受人 | 待填写 |
| 发布记录文件 | 待填写 |
| 回退使用的版本与备份 | 待填写 |
| 发布耗时 / 回退耗时 | 待填写 |
| 回退后冒烟与 UAT | 待填写 |
| 业务验收人 | 待签署 |
| 运维验收人 | 待签署 |

## 6. 当前执行记录

2026-07-29 在当前完整容器环境执行基础 UAT，8 项自动检查全部通过，耗时 2.27 秒。已验证登录与当前会话、
创建单型号审核任务、资料上传、Celery 异步解析、解析证据块追溯和人工接受解析质量。

同日完成隔离环境回退机械演练：联合备份位于 `/tmp/coal-rollback-drill/20260729T213000Z`，数据库与约
116 MiB MinIO 对象的 SHA-256 校验通过；切换 Commit `cf2a72a` 预构建的上一版本镜像并整体恢复后，任务数
由备份后含标记任务的 103 回到备份点的 102。严格生产冒烟和基础 UAT 8 项通过，回退耗时 39 秒，成功记录为
`/tmp/coal-rollback-drill/release-records/20260729T135014Z-rollback.env`。该次使用 `--local-drill`，不作为目标
环境不可变摘要回退签署证据；演练后已恢复当前候选镜像并再次通过严格冒烟。

完整 UAT 所需标准、解析修订、规则、执行器和规则包已装载，API/Worker 的 `model-egress` 也已验证可到达
千帆服务。活动文本模型连通性测试收到 HTTP 401 `invalid_iam_token`，说明当前 IAM Token 被供应商拒绝；
完整 UAT 因此按门禁失败，未生成真实模型通过证据。轮换有效 Token 后须重新运行完整 UAT，再进行业务与运维签署。
