route:
  receiver: operations-webhook
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operations-webhook
    webhook_configs:
      - url: http://alert-notifier:8080/alerts
        send_resolved: true
